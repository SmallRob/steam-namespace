# Steam数据集成项目依赖
# 核心依赖
requests>=2.25.0    # HTTP请求（Steam Web API调用）
pandas>=1.3.0      # 数据处理和CSV/Excel导出
urllib3>=1.26.0     # HTTP库（requests的依赖）

# 现有项目依赖
beautifulsoup4>=4.9.0  # HTML解析（SteamDB数据抓取）
selenium>=4.0.0    # 浏览器自动化（SteamDB登录）

# 可选依赖
openpyxl>=3.0.0    # Excel文件处理（可选）

# 测试依赖（可选）
pytest>=6.0.0
